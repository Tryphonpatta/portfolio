export type User = {
  username: string;
  name: string;
  birthday: string;
  avatar: string;
  ranking: number;
  solved: number;
};

export type AcceptedSubmission = {
  lang: string;
  title: string;
};
